# lesson formats

lesson formats are in markdown, and the SQL tables themselves are generated from code blocks in the markdown file, via JSON schemas with [json-schema-faker](https://github.com/json-schema-faker/json-schema-faker)